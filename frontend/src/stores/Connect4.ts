// import { Board } from "types/game";
// import { generateBoard } from "src/utils/game.ts";

// TODO: Rewrite all game logic to class
// using states to control board logic was fun but a bad idea LOL

export class Connect4 {
  // private row: number;
  // private col: number;
  // private Board: Board;
  // constructor(row: number, col: number) {
  //   this.Board = generateBoard(row, col);
  //   add override or function where i can send boardhistory
  // }
}
